export function UsageMeter({ label, value, limit }: { label: string; value: number; limit: number }) { const percent = Math.min(100, Math.round((value / Math.max(limit, 1)) * 100)); return <div><div className="flex justify-between text-sm font-bold"><span>{label}</span><span className="text-muted-foreground">{value}/{limit}</span></div><div className="mt-3 h-2.5 rounded-full bg-muted"><div className={`h-2.5 rounded-full transition-[width] duration-500 motion-reduce:transition-none ${percent >= 80 ? "bg-amber-500" : "bg-primary"}`} style={{ width: `${percent}%` }} /></div></div>; }
