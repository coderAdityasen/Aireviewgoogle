export function ActivityFeed({ items }: { items: Array<{ label: string; detail?: string; createdAt: string }> }) { return <div className="space-y-3">{items.length ? items.map((item, index) => <div key={`${item.createdAt}-${index}`} className="flex gap-3"><span className="mt-1.5 h-2 w-2 shrink-0 rounded-full bg-primary" /><div><p className="text-sm font-medium">{item.label}</p><p className="text-xs text-muted-foreground">{item.detail ?? new Date(item.createdAt).toLocaleString()}</p></div></div>) : <p className="text-sm text-muted-foreground">No activity yet.</p>}</div>; }
