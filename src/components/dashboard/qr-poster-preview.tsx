"use client";
/* eslint-disable @next/next/no-img-element -- QR data URLs are generated in the browser. */
export function QrPosterPreview({ qrDataUrl, businessName, headline, brandColor, template = "light" }: { qrDataUrl?: string; businessName: string; headline: string; brandColor: string; template?: "light" | "dark" }) { return <div className={`aspect-[1/1.414] rounded-2xl p-8 text-center ${template === "dark" ? "bg-[#0b1730] text-white" : "bg-white text-slate-950"}`} style={{ borderTop: `8px solid ${brandColor}` }}><p className="text-xs font-medium uppercase tracking-[0.2em] opacity-60">{businessName}</p><h3 className="mt-8 text-2xl font-semibold">{headline}</h3>{qrDataUrl ? <img src={qrDataUrl} alt="QR code poster preview" className="mx-auto mt-10 h-48 w-48" /> : <div className="mx-auto mt-10 h-48 w-48 rounded-xl bg-black/10" />}<p className="mt-8 text-sm opacity-70">Scan to share your genuine experience</p></div>; }
