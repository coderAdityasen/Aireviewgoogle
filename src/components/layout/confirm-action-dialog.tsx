"use client";
import { useState } from "react";
import { Button } from "@/components/ui/button";
export function ConfirmActionDialog({ label, confirmation, onConfirm }: { label: string; confirmation: string; onConfirm: () => Promise<void> | void }) { const [open, setOpen] = useState(false); const [value, setValue] = useState(""); return open ? <div role="dialog" aria-modal="true" className="rounded-xl border bg-card p-4 shadow-lg"><p className="text-sm">Type <strong>{confirmation}</strong> to continue.</p><input autoFocus value={value} onChange={(event) => setValue(event.target.value)} className="mt-3 h-9 w-full cursor-text rounded border px-2" /><div className="mt-3 flex gap-2"><Button variant="outline" onClick={() => setOpen(false)}>Cancel</Button><Button variant="destructive" disabled={value !== confirmation} onClick={() => { void onConfirm(); setOpen(false); }}>{label}</Button></div></div> : <Button variant="destructive" onClick={() => setOpen(true)}>{label}</Button>; }
