import { LazyActivityChart } from "@/features/analytics/components/lazy-activity-chart";
import { getOwnerDashboardMetrics } from "@/features/analytics/server/queries";
import { MetricCard } from "@/components/layout/metric-card";
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
import { Icon } from "@/components/ui/icon";
export default async function AnalyticsIndexPage({ searchParams }: { searchParams: Promise<{ range?: string }> }) { const { range } = await searchParams; const days = range === "7" ? 7 : range === "90" ? 90 : 30; const metrics = await getOwnerDashboardMetrics(days); return <div className="space-y-6"><div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4"><MetricCard label="Total QR Scans" value={metrics.counts.qr_scan} hint="Tracked QR and campaign scans" icon={<Icon name="qr" className="h-6 w-6" />} iconClassName="text-slate-500" /><MetricCard label="Google Review Page Opens" value={metrics.counts.google_redirect_clicked} hint="A page open does not confirm publication" icon={<Icon name="externalLink" className="h-6 w-6" />} iconClassName="text-blue-600" /><MetricCard label="Average Star Rating" value={metrics.averageRating ? `${metrics.averageRating.toFixed(1)}★` : "—"} hint="Based on customer-selected ratings" icon={<Icon name="star" className="h-6 w-6 fill-current" />} iconClassName="text-amber-500" /><MetricCard label="Private Feedback" value={metrics.counts.private_feedback_submitted} hint="Direct customer messages" icon={<Icon name="message" className="h-6 w-6" />} iconClassName="text-rose-500" /></div><Card><CardHeader><CardTitle>{days}-day activity</CardTitle></CardHeader><CardContent><LazyActivityChart data={metrics.activity} /></CardContent></Card></div>; }
