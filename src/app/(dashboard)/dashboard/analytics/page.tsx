import { LazyActivityChart } from "@/features/analytics/components/lazy-activity-chart";
import { getOwnerDashboardMetrics } from "@/features/analytics/server/queries";
import { MetricCard } from "@/components/layout/metric-card";
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
export default async function AnalyticsIndexPage({ searchParams }: { searchParams: Promise<{ range?: string }> }) { const { range } = await searchParams; const days = range === "7" ? 7 : range === "90" ? 90 : 30; const metrics = await getOwnerDashboardMetrics(days); return <div className="space-y-6"><div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4"><MetricCard label="QR scans" value={metrics.counts.qr_scan} /><MetricCard label="Unique visitors" value={metrics.uniqueVisitors} /><MetricCard label="AI drafts generated" value={metrics.counts.review_generated} /><MetricCard label="Opened Google review page" value={metrics.counts.google_redirect_clicked} hint="A page open does not prove a review was published." /></div><Card><CardHeader><CardTitle>{days}-day activity</CardTitle></CardHeader><CardContent><LazyActivityChart data={metrics.activity} /></CardContent></Card></div>; }
